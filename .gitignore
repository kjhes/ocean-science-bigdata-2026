# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# 비밀키 (ROMS API 인증키 등)
.env

# data/raw/ 는 현재 비어있음 (실제 원자료는 저장소 루트의 '수온 데이터/' 폴더 사용)
# 팀 공유를 위해 원자료·전처리 결과·차트 모두 git에 포함 (data/processed, reports/figures 제외 안 함)

# 전달용 ZIP 패키지 (내용은 저장소에 병합됨)
*.zip

# 실험 중간 결과(예측값 원본) - 스크립트로 다시 만들 수 있고 용량이 커서 제외 (요약표는 포함)
reports/buoy_validation/predictions.csv
reports/long_history_experiment/predictions.csv
reports/pooled_experiment/predictions.csv
reports/recursive_experiment/predictions.csv
reports/spatial_experiment/loso_predictions.csv
reports/improve_experiment/errors.csv
reports/improve_experiment/crossing.csv

# 국립수산과학원 양식기술 매뉴얼 원문 PDF (합계 약 700MB, nifs.go.kr/cmmn/file/farm/ 에서 다시 받을 수 있음)
docs/reference/nifs_manuals/

# 국립수산과학원 양식기술 매뉴얼 원본 PDF (합계 약 700MB, 전복 매뉴얼은 GitHub 파일 한도 초과)
# 받는 곳: https://www.nifs.go.kr/cmmn/file/farm/farm_번호.pdf  (번호·쪽은 docs/어종별_기준수온_조사.md 참고)
docs/reference/nifs_manuals/
